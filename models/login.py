class Login:
    def __init__(self, login_id, email, password, usuario_id):
        self.login_id = login_id
        self.email = email
        self.password = password
        self.usuario_id = usuario_id
